parameters:
    tasks:
      codeception: null
      phpmd:
        whitelist_patterns:
          - /^src\/(.*)/
        ruleset:
          - ./ruleset.xml
      phpcs:
        whitelist_patterns:
          - /^src\/(.*)/
